# 留待以后处理

保留已定型的结果，不再执行本地或远程修改。这是成功的非整合性交接，而不是交付。

复核源分支仍指向交付提交头、其树是已审查树、工作树干净、发布状态与契约一致，并且每个恢复引用仍指向所记录的对象。对于**已交付**，复核目标证明，并让源和目标保持原样。

冻结已证明且分类为 `non-integrating handoff` 的 `outcome_result`，以及把每个项目记录为有意保留的 `cleanup_result`。返回状态 `complete`、目标边界、源身份、历史策略、审查和验证绑定、发布、保留的恢复项、生命周期责任方，以及一个准确的后续整合或清理操作。如果在权威目标上重新证明**已交付**，则把结果分类为权威交付。保留是选定结果，而不是失败的清理。
