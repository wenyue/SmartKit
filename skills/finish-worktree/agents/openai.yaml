interface:
  display_name: "Finish Worktree"
  short_description: "Finalize or hand off a reviewed worktree"
  default_prompt: "Use $finish-worktree either to finalize or hand off this reviewed isolated linked Git worktree, or to carry out an explicit request to discard one exact isolated worktree and its local state."
