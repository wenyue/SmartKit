interface:
  display_name: "Create Worktree"
  short_description: "Select or create an isolated Git worktree safely"
  default_prompt: "Use $create-worktree to select or create and prepare one isolated linked Git worktree for this implementation scope."
