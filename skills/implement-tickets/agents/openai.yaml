interface:
  display_name: "Implement Tickets"
  short_description: "Run a Ticket Batch in one worktree"
  default_prompt: "Use $implement-tickets to implement the remaining agent-ready tickets sequentially."
policy:
  allow_implicit_invocation: true
