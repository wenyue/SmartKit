diff --git a/SKILL.md b/SKILL.md
--- a/SKILL.md
+++ b/SKILL.md
@@ -30,13 +30,16 @@

 - Each slice cuts a narrow but COMPLETE path through every layer (schema, API, UI, tests) — vertical, NOT a horizontal slice of one layer
 - A completed slice is demoable or verifiable on its own
-- Each slice is sized to fit in a single fresh context window
-- Any prefactoring should be done first
+- Give each slice enough cohesive work to justify independent implementation and review, sized for a current frontier LLM agent to reliably understand, implement, verify, and repair within one fresh context using the available tools. Treat the context limit as a capacity ceiling; keep work that fits comfortably together.
+- Place ticket boundaries where they provide independently meaningful acceptance, useful parallel execution, separation of materially different ownership or risk, a capability or context boundary, or a required green boundary in an expand-contract migration.
+- Do prefactoring before the change that consumes it, keeping incidental or local prefactoring in the first consuming ticket. Give it a separate ticket only when it is independently reusable, needed by multiple later tickets, materially risky, or beyond the reliable capability or context bound.

 </vertical-slice-rules>

 Give each ticket its **blocking edges** — the other tickets that must complete before it can start. A ticket with no blockers can start immediately.

 **Wide refactors are the exception to vertical slicing.** A **wide refactor** is one mechanical change — rename a column, retype a shared symbol — whose **blast radius** fans across the whole codebase, so a single edit breaks thousands of call sites at once and no vertical slice can land green. Don't force it into a tracer bullet; sequence it as **expand–contract**. First expand: add the new form beside the old so nothing breaks. Then migrate the call sites over in batches sized by blast radius (per package, per directory), each batch its own ticket blocked by the expand, keeping CI green batch to batch because the old form still exists. Finally contract: delete the old form once no caller remains, in a ticket blocked by every migrate batch. When even the batches can't stay green alone, keep the sequence but let them share an integration branch that all block a final integrate-and-verify ticket — green is promised only there.
+
+Before presenting the draft for approval, rebalance in both directions: merge undersized or tightly coupled adjacent candidates, and split candidates beyond reliable frontier-agent capacity. Finish when every ticket has coherent scope and distinct acceptance criteria, and every boundary is justified by the factors above.

 ### 4. Quiz the user
